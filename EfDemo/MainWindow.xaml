<Window x:Class="EfDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:windows="clr-namespace:EfDemo.View_Models.Windows"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525" d:DataContext="{d:DesignInstance windows:MainWindowVm}">
    
        <TabControl HorizontalAlignment="Left">
            <TabItem Header="Courses" DataContext="{Binding CoursesVm}">
                <Grid Background="#FFE5E5E5"/>
            </TabItem>
            <TabItem Header="Departments" DataContext="{Binding DepartmentsVm}">
                <Grid Background="#FFE5E5E5"/>
            </TabItem>
        <TabItem Header="Districts" DataContext="{Binding DistrictsVm}">
            <Grid Background="#FFE5E5E5"/>
        </TabItem>
        <TabItem Header="Schools" DataContext="{Binding SchoolsVm}">
            <Grid Background="#FFE5E5E5"/>
        </TabItem>
        <TabItem Header="Students" DataContext="{Binding StudentsVm}">
            <Grid Background="#FFE5E5E5"/>
        </TabItem>
        <TabItem Header="Teachers" DataContext="{Binding TeachersVm}">
            <Grid Background="#FFE5E5E5"/>
        </TabItem>
    </TabControl>
</Window>
